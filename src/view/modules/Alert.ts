/**Created by the LayaAirIDE*/
module view.modules {
	import WebGL = Laya.WebGL;
	import Loader = laya.net.Loader;
	import Handler = laya.utils.Handler;

	export class Alert extends ui.modules.AlertUI {
		constructor() {
			super();
		}

		public static show() {

		}
	}
}